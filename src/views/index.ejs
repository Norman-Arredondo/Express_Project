<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="<%= base_url %>css/index.css">
</head>
<body>
    Raiz del proyecto pero con HTML
    <%= base_url %>

    <% frutas.forEach(fruta => { %>
            <% if (fruta === "manzana"){ %>
                <p style="font-weight: bold;"><%= fruta %></p>
            <% }else{ %>
                <p><%= fruta %></p>
            <% } %>
    
          <% }); %>




    


</body>
</html>